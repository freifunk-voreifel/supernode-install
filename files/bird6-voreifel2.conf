# Configure logging
#log syslog { debug, trace, info, remote, warning, error, auth, fatal, bug };
#log stderr all;
#log "tmp" all;
#log syslog all;

#debug protocols all;

# Override router ID
router id 10.152.64.2;

protocol direct {
        interface "bat0", "gre-*", "lo";  # Restrict network interfaces it works with

}


protocol kernel {
        device routes;
        import all;
        export all;             # Default is export none
        kernel table 42;                # Kernel table to synchronize with (default: main)
}

protocol device {
        scan time 10;           # Scan interfaces every 10 seconds
}

function is_default() {
        return (net ~ [::/0]);
}

# own networks # angepass voreifel
function is_self_net() {
return net ~ [ fda4:1721:8451:1::/64+ ];
}

# freifunk ip ranges in general
function is_freifunk() {
return net ~ [ fc00::/7{48,64},
2001:bf7::/32+];
}

filter hostroute {
        if net ~ 2a03:2260:301a:1000::/52 then accept;
        reject;
}



# Uplink zum FF Rheinland
template bgp uplink {
        local as 65201;
        import where is_default();
        export filter hostroute;
        gateway recursive;
}


protocol bgp ffrl_bb_a_ak_ber from uplink {
        source address 2a03:2260:0:524::2;
        neighbor 2a03:2260:0:524::1 as 201701;
}

protocol bgp ffrl_bb_b_ak_ber from uplink {
        source address 2a03:2260:0:527::2;
        neighbor 2a03:2260:0:527::1 as 201701;
}


protocol bgp ffrl_bb_a_ix_dus from uplink {
        source address 2a03:2260:0:526::2;
        neighbor 2a03:2260:0:526::1 as 201701;
}

protocol bgp ffrl_bb_b_ix_dus from uplink {
        source address 2a03:2260:0:529::2;
        neighbor 2a03:2260:0:529::1 as 201701;
}

protocol bgp ffrl_bb_a_fra2_fra from uplink {
        source address 2a03:2260:0:525::2;
        neighbor 2a03:2260:0:525::1 as 201701;
}

protocol bgp ffrl_bb_b_fra2_fra from uplink {
        source address 2a03:2260:0:528::2;
        neighbor 2a03:2260:0:528::1 as 201701;
}
