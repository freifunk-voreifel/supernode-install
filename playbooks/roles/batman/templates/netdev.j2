# The L2TP tunnels will be assigned to this bridge.
# The bridge itself is assigned to the batman interface
# Create bridge by networkd instead in a tunneldigger session_up script.  
[NetDev]
Name=mesh-{{ hoods[item.0].mesh.iface }}
Kind=bridge
